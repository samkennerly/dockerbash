#!/usr/bin/env bash

# Run a self-destructing, interactive container from selected image.
# Example: runit python
# Example: runit python:3.7
# Example: runit python:3.7 ls -la

docker run --interactive --tty --rm "$@"
